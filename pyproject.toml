[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "value-investing-heuristics"
version = "0.1.0"
description = "Value investing portfolio screening with heuristic optimization and leak-aware backtesting."
requires-python = ">=3.11"
dependencies = [
  "pandas>=2.2",
  "numpy>=2.0",
  "requests>=2.32",
  "beautifulsoup4>=4.12",
  "lxml>=5.0",
  "yfinance>=0.2.40",
  "matplotlib>=3.9",
  "tabulate>=0.9",
  "flask>=3.0",
  "flask-cors>=4.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
vih = "value_investing_heuristics.cli:main"
vih-web = "value_investing_heuristics.web.server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
value_investing_heuristics = ["web/static/**/*", "web/templates/**/*"]
